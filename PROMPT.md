幫我使用 Python 網站建立一個 WEB 用來做股票價格的顯示

**專案範圍限制：**
- 僅支援單一股票查詢（不需要股票清單或歷史資料）
- 不使用資料庫：所有資料都在記憶體中處理
- 不需要使用者認證：無登入、註冊功能
- 不需要快取
- 不需要微服務架構：單一 Flask 應用程式
- 不需要複雜的設定管理：使用簡單的 config.py
- 不需要生產級部署：僅考慮開發環境執行
- 機密或設定值放在 .env 裡面

**Git 版本控制與專案管理：**
- Repository: https://github.com/lettucebo/20250805-AZ2007
- 使用功能分支開發流程（feature/<branch-name>）
- 遵循 Conventional Commits 標準
- 建立對應的 GitHub Issues 追蹤開發進度
- 每個步驟都要分開進行 GIT 提交後 PUSH 上去，以保留歷史紀錄

站在使用者功能的角度:
1. 使用者可以透過簡單的介面輸入股票代碼。
2. 系統會即時顯示該股票的最新價格。
3. 提供自動刷新功能，讓使用者隨時掌握最新股價動態。
   1. 使用者可以設定刷新頻率，例如每10秒、每分鐘或每小時自動更新價格。

站在 UI 設計的角度:
- 簡潔明瞭的介面，讓使用者能夠輕鬆輸入股票代碼。
- 實時顯示股票價格，並提供明確的刷新頻率設定選項。
- 使用 Bootstrap 5 框架來美化介面。
- 使用 fontawesome 圖示來增強使用者介面。
- 提供響應式設計，確保在不同裝置上都有良好的使用體驗。
- 提供 Light Mode 與 Dark Mode 切換功能。

站在系統設計的角度:
- 開發環境是 Windows 11/
- 使用 Python 3.11.9
- 使用 Flask 框架來建立 Web 應用程式。
- 整合第三方股票價格 API 獲取最新價格資訊。
- 使用虛擬環境管理 Python 依賴包。
  -  虛擬環境名稱: stock05env

**API 選擇評估標準：**
- 必須支援台股查詢（如 2330.TW、2454.TW）
- 免費額度需足夠 DEMO 展示使用
- 註冊流程簡單，無需信用卡驗證
- API 回應格式清晰，適合快速整合
- 提供每個 API 的實際測試結果與回應範例

**錯誤處理需求：**
- API 請求失敗時的優雅降級
- 無效股票代碼的使用者提示
- 網路連線異常的處理機制
- 請求頻率限制的提示訊息

**測試策略：**
- 專案必須包含單元測試
  - 使用 pytest 作為測試框架
  - 測試應涵蓋主要功能，包括股票價格查詢
  - 單元測試：API 服務、路由處理
  - 整合測試：端到端 API 呼叫
  - 模擬 API 回應進行離線測試

**視覺化圖表需求（使用 Mermaid）：**
請提供以下 Mermaid 圖表來說明專案架構與流程：

1. **系統架構圖 (Graph)**
   - 展示前端、後端、API 服務之間的關係
   - 包含主要元件與資料流向

2. **使用者操作流程圖 (Flowchart)**
   - 從輸入股票代碼到顯示價格的完整流程
   - 包含錯誤處理分支

3. **API 呼叫時序圖 (Sequence Diagram)**
   - 前端 → Flask 後端 → 第三方 API → 回傳資料的時序
   - 包含自動刷新機制

4. **專案檔案結構圖 (Graph)**
   - 視覺化專案目錄結構
   - 展示檔案之間的依賴關係

5. **錯誤處理流程圖 (Flowchart)**
   - 各種錯誤情況的處理邏輯
   - API 失敗、網路異常、無效股票代碼等情境

**文件與實作計畫要求：**
- 在 /docs 目錄下維護相關文件
- 保持 README.md 與新功能同步
- 在 CHANGELOG.md 中維護變更記錄
- 在 /docs/implementation 中撰寫實作計畫
  - 命名規則：<date>-<feature-name>.md
  - 每個功能的實作計畫都要有詳細的步驟說明
  - 每個步驟都要有明確的目標與預期結果
  - 每個步驟都要有明確的驗收標準
  - 每個步驟都要有明確的測試計畫
- 在 /docs/progress 中記錄開發進度與狀態
- 在 /docs/architecture 中維護架構設計文件

**分階段開發策略：**
- 將專案分為 4-5 個階段進行，每階段都要有詳細的 PROMPT 指導
- 第一階段：專案架構設計與技術分析
- 第二階段：後端核心功能實作
- 第三階段：前端介面開發
- 第四階段：測試與文件撰寫
- 第五階段：整合與優化（可選）

**架構分析與圖表要求：**
- 請提供詳細的系統架構分析，包含技術棧選擇理由與替代方案比較
- 必須提供完整的 Mermaid 架構圖表，包含：
  - 系統整體架構圖（前端層、後端層、外部服務層）
  - 詳細資料流程圖（包含快取邏輯和錯誤處理）
  - 應用程式生命週期圖
  - 前端使用者互動流程圖
  - 錯誤處理流程圖
  - 部署架構圖
  - 前端組件架構圖
- 提供技術決策記錄文件，說明每個技術選擇的理由
- 包含設計模式應用說明（工廠模式、服務層模式等）
- 提供效能考量和安全性架構說明

**程式碼品質與結構要求：**
- 提供完整的專案目錄結構，包含用途說明
- 所有檔案都要有完整的程式碼實作
- 包含 requirements.txt 和開發依賴清單
- 提供 .gitignore（Python + Windows）
- 包含 pytest.ini 和測試設定
- 程式碼要有類型提示（Type Hints）
- 單元測試覆蓋率要求：整體 > 80%，核心邏輯 > 95%

**文件要求：**
- 要有對應的 README.md 檔案，來說明如何啟動網站等等常見的 README 內容
  - 包含安裝依賴、啟動伺服器、使用說明等資訊
  - 包含如何執行單元測試與顯示詳細測試內容說明
  - 快速開始指南（5分鐘內可執行）
  - API Key 設定步驟
  - 常見問題排解
  - 專案架構說明
  - **將所有 Mermaid 圖表嵌入到 README.md 中**
- 程式碼應該要有適當的註解
  - Class 與 Method 一定要有註解
  - 所有的變數與參數一定要有註解
  - 適當的對所有的程式碼加入註解
  - 註解語言請使用英文

**其他要求:**
- 所有的回應與輸出請使用繁體中文
- 程式碼註解請使用英文
- Git 提交訊息請使用英文並遵循標準格式
- 如果有功能無法實作，請明確說明原因和替代方案

**重要：這是一個 DEMO 網站專案，請保持功能簡單但架構要詳細：**
DEMO 的核心價值在於「它能正常工作」，而不是「它有很多功能」。

先不要執行，請先產出一個執行計畫與檔案文件目錄結構給我看，以及提供預計要使用那些 API 來取得股價資訊，幫我做詳細比較包含優缺點，並提供這些API的前置作業說明，例如: 需要註冊帳號、取得 API KEY 等等。同時請提供所有要求的 Mermaid 圖表。